# Database Configuration - Supabase PostgreSQL
# IMPORTANT: Use different databases for development and production
# Development: Use local database or development instance
# Production: Use production database with proper credentials
DATABASE_URL="postgresql://postgres:your-supabase-password@supabase.turlacu.ro:5432/postgres"

# NextAuth.js Configuration
NEXTAUTH_URL="http://localhost:3000"  # Change to your domain in production
NEXTAUTH_SECRET="your-super-secret-key-change-this-in-production"

# MinIO Configuration (S3-compatible storage)
MINIO_ENDPOINT="localhost:9000"
MINIO_ACCESS_KEY="minioadmin"
MINIO_SECRET_KEY="minioadmin123"
MINIO_BUCKET_NAME="workflowpro-storage"

# Application Environment
NODE_ENV="development"  # Use "production" for production deployment

# Optional: Redis Configuration (if using Redis for caching)
REDIS_URL="redis://localhost:6379"

# Environment-specific Database Examples:
# Development: DATABASE_URL="postgresql://dev_user:dev_pass@localhost:5432/workflowpro_dev"
# Staging:     DATABASE_URL="postgresql://stage_user:stage_pass@stage_host:5432/workflowpro_stage"
# Production:  DATABASE_URL="postgresql://prod_user:prod_pass@prod_host:5432/workflowpro_prod"